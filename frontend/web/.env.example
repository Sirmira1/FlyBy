# Mapbox GL JS access token (a PUBLIC token, starts with "pk.").
# Create one for free at https://account.mapbox.com/access-tokens/
# The NEXT_PUBLIC_ prefix exposes it to the browser, which Mapbox GL JS requires.
NEXT_PUBLIC_MAPBOX_TOKEN=
